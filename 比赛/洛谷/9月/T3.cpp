#include <cstdio>
#include <iostream>
#include <queue>
#include <algorithm>
#include <cstring>
typedef long long ll;
const int maxn = 1e6 + 5;
int a[maxn];
int n;
using namespace std;
int main() {
    freopen("data.in", "r", stdin);
    cin >> n;
    for (int i = 1;i <= n;i++) {
        scanf("%d", &a[i]);
    }
    int ans;
    printf("%lld\n", ans);
    return 0;
}